import React, { Component } from 'react';
import Counter from './Counter'

// create a component
class App extends Component {
    render() {
        return (
            <Counter />
        );
    }
}

export default App;
